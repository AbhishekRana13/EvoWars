import * as PIXI from 'pixi.js';

export const Globals = {
    resources: {},
    world : null,
    worldBounds : new PIXI.Bounds()
};

export const gameSettings = {
    speed : 4,
    boostedSpeed : 8
};